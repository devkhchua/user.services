server:
  port: 0
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user-services
    username: root
    password: watashiwa
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
  application:
    name: auth-service

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

management:
  endpoints:
    web:
      exposure:
        include: "*"